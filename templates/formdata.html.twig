{% extends 'partials/base.html.twig' %}

{% block content %}

    {{ content }}

    <div class="alert">{{ form.message }}</div>
    <p>Your submission:</p>

    {% include "forms/data.html.twig" %}

{% endblock %}
